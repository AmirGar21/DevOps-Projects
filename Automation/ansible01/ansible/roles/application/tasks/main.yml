---
# tasks file for roles/application
    - name: Downloading neccesary packages
      become: yes
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg-agent
          - software-properties-common
        state: present
        update_cache: yes

    - name: Adding gpg key to Docker
      become: yes
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Adding Docker repository
      become: yes
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release | lower }} stable"
        state: present

    - name: Updating apt cache
      become: yes
      apt:
        update_cache: yes

    - name: Installing Docker
      become: yes
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
        state: latest


    - name: Ensure /home/vagrant/ansible01 exists
      become: yes
      file:
        path: /home/vagrant/ansible01
        state: directory
        mode: '0755'
        owner: vagrant
        group: vagrant
    - name: Ensure Docker service is running
      service:
        name: docker
        state: started
        enabled: yes

    - name: Copying docker-compose.yml
      become: yes
      copy:
        src: /vagrant/docker-compose.yml
        dest: /home/vagrant/ansible01/docker-compose.yml

    - name: Synchronize microservice files
      become: yes
      synchronize:
        src: /vagrant/services
        dest: /home/vagrant/ansible01/
        recursive: yes

    - name: Deployment of the microservice application
      become: yes
      command: docker compose up -d
      args:
        chdir: /home/vagrant/ansible01
